server:
  port: 8080
spring:
  application:
    name: server
  config:
    import:
      - "optional:configserver:http://localhost:8090/"
management:
  endpoints:
    web:
      exposure:
        include:
          - "*"
